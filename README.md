Playground for RxJS v6 operators

### Run an operator on cli

```
$ npm run exec <operator>
```

For example:
```
$ npm run exec withLatestFrom
```

### Run an operator in the browser

Some operator can be run in browser:

```
$ npm run serve 
```

Go to http://localhost:8080